# Claude Opus 5 독립 검토 요청: 2차 검토 후 수정 확인

## 기준과 검토 범위

이 PR은 `833b85d`를 기준으로 한 심사보고서 후속 수정이며, 이번 실행은 PR #9의
Claude Opus 5 1·2차 검토에서 나온 F1--F11과 G1--G11을 반영한 3차 독립 검토다. 최종 범위는
항상 `base...HEAD` diff다. 이 문서와 실제 diff가 다르면 증거 무결성 문제로
보고한다.

## 1차 검토 후 실질 수정

1. **Harville 명칭 충돌(F1)**: 순위검증 표는 같은 단조보정을 공유하는
   `보정 Harville(α=1)`과 `보정·단계조정 Harville`로, 동일표본 가격표는
   `게시가격 Harville(비보정)`과 `보정·단계조정 Harville`로 구분했다.
2. **착순 무결성(F2)**: 가격 전용 Panel B가 착순을 요구하지 않게 했고,
   clean 외부검증에서는 고유 상위 3착, 승식별 단일 `is_hit`, 파싱착순과
   적중조합의 일치를 전수검사한다. 3,321경주에서 위반은 0건이다.
3. **집계·추론(F3)**: proper log score의 기대손실에 맞춘 경주균등 평균과
   경주일 군집 bootstrap을 주결과로 유지하되, 사전 주패널 규칙과 비교할 수
   있도록 개선폭 중앙값과 경주 bootstrap을 병기했다. EPSILON 구속 건수를
   모형별로 공개한다.
4. **대칭 보정(F4)**: 두 가격측도 각각에 다른 연도의 상태별 예측가격과
   실현지시자로 단조보정을 추정하고 검증연도 경주 안에서 재정규화했다.
   보정 뒤 평균 개선은 쌍승 0.0275[0.0062, 0.0486], 복승
   0.0235[0.0026, 0.0436], 삼복승 0.0423[0.0196, 0.0665]이고 단승은
   0.0051[-0.0162, 0.0242]다.
5. **보고 위계(F5)**: 실현착순 결과를 주분석 중간에서 제거해 행동모형 진단
   뒤의 독립된 마지막 결과절로 옮겼다.
6. **보조표(F6--F7)**: 경주 수와 JS 경주 bootstrap 구간을 추가하고,
   Harville 대비 강건성으로 해석을 한정했으며 단승 항등행을 캡션에 명시했다.
7. **승식 간 TV(F8)**: 단순 수준 비교를 구조적 결론으로 사용하지 않고 균등
   기준 상대오차를 보조 기술통계로 제시하면서 차원 의존성을 남겼다.
8. **응답서·가용성·계약(F9--F11)**: Biometrika는 심사보고서 표기의 교정임을
   명시했고, 검사와 재생성 명령을 분리했으며 RESEARCH_PLAN의 산출물 목록을
   Paper CI 동결 경로에 맞췄다.
9. **직접 진입점 정합성**: `analysis.main_analysis_runner`에 빠져 있던
   `if __name__ == "__main__": main()`을 복구했다. Paper CI는 원래부터 guard가
   있는 `analysis.main_analysis`를 실행했으므로 과거 CI 계산은 유효하다. 이
   수정은 README에 문서화된 직접 runner 호출의 재현성만 복구한다.

## 2차 검토 후 실질 수정

1. 남아 있던 본문·README의 Harville 명칭을 실제 입력과 일치시켰다.
2. 동착·복수 적중·파싱 불일치는 Panel A 가격거리 계산을 중단하지 않고
   로그점수에서만 사유와 함께 제외하며, 후보·유효·제외 경주 수를 출력한다.
3. 평균·날짜군집 규칙이 심사 대응 단계에서 결과와 함께 추가되었음을 공개하고,
   평균과 중앙값을 대등하게 보고한다. 단승의 상충 결과와 삼복승 꼬리영향도
   본문에 명시했다.
4. 보정 확률이 EPSILON에 닿은 어느 모형의 경주도 제외하는 민감도를 추가했다.
   세 복합승식에서 각 2경주를 제외해도 양의 개선과 날짜군집 구간이 유지된다.
5. 교차적합 보정의 검증연도 비누출, 경주별 합 1, 동일 입력의 모형 대칭성을
   단위테스트로 고정했다.
6. 외부 로그점수 CSV는 부동소수점 허용오차와 레이블·건수 exact 검사를 분리해
   환경별 무해한 바이트 차이를 허용하고, 나머지 표·산출물은 exact 비교한다.
7. 주분석 전체 재생성 명령을 README에 추가하고, 기계 레이블을 한글 표기로
   바꾸며 상관계수·cosine을 거리 아닌 형태 유사도로 설명했다.

## 반드시 대조할 결과

- 미보정 평균 개선: 쌍승 0.0317, 복승 0.0285, 삼복승 0.0759, 단승 -0.0005.
- 미보정 중앙 개선: 쌍승 0.0272, 복승 0.0132, 삼복승 0.0160, 단승 0.0164.
- 보정 평균 개선: 쌍승 0.0275, 복승 0.0235, 삼복승 0.0423, 단승 0.0051.
- 보정 EPSILON 관측을 제외한 평균 개선: 쌍승 0.0277, 복승 0.0239,
  삼복승 0.0423, 단승 0.0051.
- 미보정 EPSILON 구속은 전 승식·두 모형 모두 0건이다. 보정 뒤 세 복합승식은
  main과 Harville 각각 1건, 단승은 각각 0건이다.
- CSV 원천은 `outputs/main_external_log_scores.csv`, 표는
  `tables/main_external_log_scores.tex`이다.
- 보조표 원천은 `outputs/main_panel_a_summary.csv`, 표는
  `tables/main_panel_a_auxiliary.tex`이다.

## 3차 핵심 질문

1. 세 Harville 명칭과 실제 코드 입력이 표·캡션·본문 전체에서 일치하는가?
2. `is_hit`·착순·동착 검사가 네 승식의 지급사건을 정확히 검증하며 가격 전용
   Panel B의 포함규칙을 불필요하게 바꾸지 않는가?
3. 대칭 연도제외 단조보정이 훈련·검증을 엄격히 분리하고, 각 경주에서 합이
   1이 되며, 두 모형에 같은 규칙을 적용하는가?
4. 평균+날짜군집과 중앙값+경주 bootstrap의 역할 구분 및 EPSILON 구속 보고가
   통계적으로 정직한가?
5. `analysis.main_analysis`의 기존 CI 계산과 직접 runner 진입점 복구의 역할을
   문서가 정확히 구분하는가?
6. 1·2차 F1--F11, G1--G11 가운데 남은 medium 이상 결함이 있는가?
7. 새 표·긴 모형명·독립 결과절 때문에 표 넘침, 글리프 문제, 빈 페이지,
   고아 캡션, 참고문헌 분할이 생기지 않았는가?

Claude는 독립 reviewer이며 파일 수정·커밋·푸시·라벨·병합을 하지 않는다.
남은 실질 결함만 보고하고, 단순 표현 취향은 findings에 포함하지 않는다.
